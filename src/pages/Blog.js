import React from 'react';
import Buttons from '../component/Buttons';

const Blog = () => {
    return (
        <div>
            <h1>blog</h1>
            <Buttons left={"/contact"}/>
        </div>
    );
};

export default Blog;